# Stage A validation

Run on 2026-09-10 in `/Users/oobi/Documents/gpt11/assay-stage-a`, based on
`858deeb`.  Command: `zsh -f dev/gates.sh`.  All 11 legs pass.  The log files
retain full stdout and stderr for each leg; gates.log retains the summary.

The review round of 2026-09-10 changed the driver interface and four gate
scripts, so every log here was regenerated by one further `zsh -f dev/gates.sh`
run on a copy of the staged tree, at
`/Users/oobi/Documents/assay-stage-a-review/fix-copy-1`.  That run passes all
11 legs, with `DRIVER cases=24 OK` and `MUTANTS killed=13/13 OK`.

This validates Stage A only.  Backend stages B-F remain pending.  The carry
row verifies all 31 lib and surface paths against kanon `2c2e6e6`.
